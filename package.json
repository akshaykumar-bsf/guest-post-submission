{
  "name": "guest-post-submission",
  "version": "1.0.0",
  "description": "Guest Post Submission WordPress Plugin",
  "scripts": {
    "test": "cypress open",
    "test:headless": "cypress run",
    "phpunit": "phpunit"
  },
  "devDependencies": {
    "cypress": "^12.0.0"
  }
}
